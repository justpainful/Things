{
  "name": "@things/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Things — the local service. Web UI on 127.0.0.1:6767, sync listener on 6768.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "node src/index.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@things/core": "*"
  }
}
