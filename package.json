{
  "name": "things",
  "version": "0.1.0",
  "private": true,
  "description": "Things — a completely local personal information system.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/core-ts",
    "apps/server",
    "apps/web",
    "tools/shots"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @things/server",
    "build": "npm run build --workspace @things/web",
    "test": "npm run test --workspace @things/core && npm run test --workspace @things/server",
    "lint": "npm run lint --workspace @things/core && npm run lint --workspace @things/server && npm run lint --workspace @things/web",
    "vectors": "node packages/core-ts/scripts/gen-vectors.ts",
    "shots:diff": "node tools/shots/diff.mjs",
    "shots:accept": "node tools/shots/diff.mjs --accept"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.7.2"
  }
}
